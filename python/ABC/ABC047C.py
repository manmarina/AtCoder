S = input().strip()
ans = 0
for i in range(1, len(S)):
    if S[i] != S[i - 1]:
        ans += 1
print(ans)

"""
着眼点

盤面を同色の連続区間（色ブロック）に分けて考えると、
色ブロックの境目=BW または WB の切れ目が存在します。
端に石を1個打つ操作は、その端に接する連続区間を反転させ、
結果として境目をちょうど1つ消すことができます（色ブロック数が1つ減る）。
全部同色にするには、境目（色の切れ目）をすべて消せばよいので、最小手数＝境目の個数です。
例：BBBWW は B|W の切れ目が1つ → 答え1。WBWBWBWBWB は切れ目が9つ → 答え9。
"""
